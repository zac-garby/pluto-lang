def $x plus $y {
  # return x + y;
  x + y;
};

# testing comments




def $x times $y {
  return x * y; # comment after line
};

def print sum of $x and $y {
  \print (x + y);
};

a = \(5) times (10) * 10;
b = \(3 * 2) plus (a);
\print sum of (a) and (b);